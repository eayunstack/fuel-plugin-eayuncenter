# ERROR: exit code 3
mongo -uadmin <%= @primary_mongo_ip %>:27017/admin -p<%= @mongo_db_password %> < <%= @db_tmp_dir %>/mongo_create_index.js
if [ $? != 0 ]; then
    echo "Mongodb init log.eayuncenter ERROR"
    exit 3
fi
